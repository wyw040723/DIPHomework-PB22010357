# Assignment 4 - Simplified 3D Gaussian Splatting

本仓库完成了第四次作业的全部代码内容，主要包括 COLMAP 结构光恢复、3D Gaussian Splatting 模型训练与渲染验证。

## 环境配置

安装依赖：

```bash
cd Assignments/04_3DGS
.venv\Scripts\python.exe -m pip install torch torchvision numpy opencv-python tqdm natsort
```

### 运行环境

- Python: 3.14.3
- 主要依赖：`torch`、`torchvision`、`numpy`、`opencv-python`、`tqdm`、`natsort`
- `pytorch3d` 已通过源码构建安装
- COLMAP 通过本地下载的 `colmap_nocuda` 二进制包在本项目下运行

### 数据准备

使用 `chair` 场景数据进行实验：

- 图像目录：`data/chair/images/`
- COLMAP 输出目录：`data/chair/sparse/0_text/`
- 训练 checkpoint 目录：`data/chair/checkpoints/`

## 任务流程

### 1. COLMAP 结构光恢复

运行命令：

```bash
.venv\Scripts\python.exe mvs_with_colmap.py --data_dir data/chair
```

执行过程包括：

- 特征提取（feature_extractor）
- 特征匹配（exhaustive_matcher）
- 稀疏重建（mapper）
- 二进制模型转换为文本格式（model_converter）

### 2. 3D Gaussian Splatting

运行命令：

```bash
.venv\Scripts\python.exe train.py --colmap_dir data/chair --checkpoint_dir data/chair/checkpoints --device cpu --num_epochs 10 --debug_every 5 --debug_samples 2
```

训练过程包括：

- 从 COLMAP 输出加载 `cameras.txt`、`images.txt`、`points3D.txt`
- 使用 `GaussianModel` 初始化 3D Gaussian 参数：位置、颜色、旋转、尺度、透明度
- 使用 `GaussianRenderer` 执行 3D Gaussian 投影、2D 高斯求值与 alpha-blending
- 训练过程中保存 checkpoint 和调试图像

### 3. 验证与渲染

运行命令：

```bash
.venv\Scripts\python.exe debug_mvs_by_projecting_pts.py --data_dir data/chair
```

该脚本将 COLMAP 恢复的稀疏 3D 点投影回原始视角，生成对比图像以验证恢复结果。

## 实验结果

### COLMAP 结果

- COLMAP 成功生成：
  - `data/chair/database.db`
  - `data/chair/sparse/0/`（二进制模型）
  - `data/chair/sparse/0_text/`（文本模型）
- `debug_mvs_by_projecting_pts.py` 成功处理所有 100 张图像，输出结果保存到 `data/chair/projections/`

### 训练结果

- 训练已在 CPU 上完成 80 个 epoch
- 训练脚本输出文件：

  - `data/chair/checkpoints/debug_rendering.mp4`


### 渲染验证

- `debug_rendering.mp4` 已生成
- `data\chair\checkpoints\render_mv_high_quality.mp4` 已生成

## 结果分析

本次实验已实现从 COLMAP 稀疏 SfM 输出到简化 3D Gaussian Splatting 的完整流水线。

- COLMAP 恢复部分运行正常，稀疏点云成功生成并投影验证通过。
- Gaussian Splatting 部分可完成前向渲染和训练流程。

## 结论

本次作业要求已基本完成：

- `mvs_with_colmap.py` 成功运行并生成 COLMAP 输出
- `debug_mvs_by_projecting_pts.py` 成功生成投影验证图
- `train.py` 可以运行完 80 个 epoch 并输出 checkpoint 与调试渲染结果